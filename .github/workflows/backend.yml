name: Backend

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_DB: cookfully_test
          POSTGRES_USER: cookfully
          POSTGRES_PASSWORD: cookfully
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U cookfully -d cookfully_test"
          --health-interval 5s --health-timeout 5s --health-retries 12
      redis:
        image: redis:8-alpine
        ports: ["6379:6379"]
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s --health-timeout 3s --health-retries 12
    env:
      COOKFULLY_ENVIRONMENT: test
      COOKFULLY_DATABASE_URL: postgresql+psycopg://cookfully:cookfully@localhost:5432/cookfully_test
      COOKFULLY_REDIS_URL: redis://localhost:6379/0
      COOKFULLY_SECRET_KEY: test-only-secret-key-with-at-least-32-characters
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.13"
          enable-cache: true
      - run: uv sync --project backend --locked --all-extras --dev
      - run: uv run --directory backend ruff format --check .
      - run: uv run --directory backend ruff check .
      - run: uv run --directory backend mypy src
      - run: uv export --project backend --locked --all-extras --no-dev --no-hashes --no-emit-project --output-file /tmp/cookfully-requirements.txt
      - run: uvx pip-audit -r /tmp/cookfully-requirements.txt --strict
      - run: uv run --directory backend pytest --cov --cov-report=term-missing
